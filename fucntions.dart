//It's recommended to specify the function's return types.

void main() {
  int addition(int numberA, int numberB) {
    int result = numberA + numberB;
    return result;
  }

  var additionResult = addition(15, 25);

  print(additionResult);
}
